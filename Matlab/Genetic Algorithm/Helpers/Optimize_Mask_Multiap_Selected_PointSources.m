function cost = Optimize_Mask_Multiap_Selected_PointSources(x)
    cost = Generate_Cost_Multiap_Selected_PointSources(x,0,0,0); %Use this function so this and the GA always have same parameters.
end